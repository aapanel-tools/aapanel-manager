-- ADR-0012 (Д-35): вид сбоя опроса хранится отдельно от слов панели, чтобы сводка парка
-- строила фразу на языке читателя. Столбец может быть пустым: сборка до этого решения его
-- не видит, а строки, записанные раньше, опрос перезапишет за один цикл.

-- AlterTable
ALTER TABLE "ServerStatus" ADD COLUMN     "errorKind" TEXT;
